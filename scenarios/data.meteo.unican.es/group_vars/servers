root: "{{ ansible_env.HOME }}/services/tds-cluster"
modproxy_conf: /etc/httpd/conf.d/modproxy.conf

jdk_home: "{{ root }}/jdk"
tomcat_home: "{{ root }}/tomcat"
tomcat_bases: "{{ root }}/{{ inventory_hostname }}/tomcats"
tomcats:
  - name: tomcat1
    http: 9080
    ssl: 9443
    ajp: 9009
    shutdown: 9005
    gateway: lb
    cluster: default
    host: "{{ ansible_all_ipv4_addresses[0] }}"
    properties: loadfactor=1 route={{ ansible_hostname }}

tds_context: thredds
