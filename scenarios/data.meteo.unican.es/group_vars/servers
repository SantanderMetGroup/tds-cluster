jdk_home: "{{ ansible_env.HOME }}/tds-cluster/jdk"
tomcat_home: "{{ ansible_env.HOME }}/tds-cluster/tomcat"
tomcat_bases: "{{ ansible_env.HOME }}/tds-cluster/{{ inventory_hostname }}/tomcats"

tomcats:
  - name: tomcat1
    http: 8080
    ssl: 8443
    ajp: 8009
    shutdown: 8005