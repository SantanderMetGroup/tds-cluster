- name: Install devel TDS ATLAS
  hosts: wn025
  roles:
    - role: devel
      tomcat: "{{ devels.atlas }}"
  tasks:
    - name: Start tomcat
      include_tasks:
        file: ../restart.yml
        apply:
          tags: start
      vars:
        tomcat: "{{ devels.atlas }}"
      tags: start
